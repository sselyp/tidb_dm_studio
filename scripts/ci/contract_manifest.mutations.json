{
  "schemaVersion": 1,
  "source": "scripts/ci/check_contract.py + scripts/ci/test_gate.py",
  "note": "Anti-shrink: CI must assert checker impl == checkFunctions, and test_gate mutation set == mutations.names (count 60).",
  "checkFunctions": [
    "check_error_code_prefix",
    "check_no_top_level_errors",
    "check_field_codes_registered",
    "check_required_field_codes",
    "check_etag_exposed",
    "check_http_codes_used",
    "check_static_routes",
    "check_oneof_write",
    "check_namepath_responses",
    "check_csrf",
    "check_me_endpoint",
    "check_login_lock",
    "check_precheck_codes",
    "check_config_storage",
    "check_state_enum_consistent",
    "check_dm_compat",
    "check_task_config_isomorphic",
    "check_allowed_actions",
    "check_no_credential_echo",
    "check_manifest",
    "check_write_mutex_codes",
    "check_ifmatch_binding",
    "check_precondition_etag",
    "check_version_description",
    "check_idempotency_optional",
    "check_diagnostic_endpoints",
    "check_diagnostic_required_fields",
    "check_diagnostic_codes",
    "check_precheck_description",
    "check_502_scope",
    "check_healthz_public"
  ],
  "mutations": {
    "count": 60,
    "names": [
      "drop 428 from PUT /tasks/{name}",
      "drop 428 from PUT /datasources/{id}",
      "drop 412 from PUT /tasks/{name}",
      "drop 412 from PUT /datasources/{id}",
      "drop ETag header from PreconditionFailed (412)",
      "drop ETag header from TaskItem",
      "drop ETag header from DataSourceItem",
      "drop 409 from DELETE /datasources/{id} (E_DS_REFERENCED)",
      "drop 409 from DELETE /tasks/{name} (state conflict)",
      "drop 502 from PUT /tasks/{name}/state (DM down)",
      "drop 404 from GET /tasks/{name}",
      "drop 400 from PUT /tasks/{name}",
      "drop 403 from POST /logout (CSRF)",
      "delete whole UpdateTask op",
      "delete whole DeleteTask op",
      "TaskConfig.sources.minItems -> 0",
      "drop TaskWrite.oneOf",
      "loosen NamePath pattern",
      "re-introduce literal /tasks/schema",
      "FieldError.errorCode.enum loses one entry",
      "info.description version text != info.version",
      "Idempotency-Key required:true on idempotent PUT",
      "re-add 422 to /task-validate (diagnostic must stay 200-only)",
      "re-add 422 to /task-precheck (diagnostic must stay 200-only)",
      "drop 422 from /datasources/test (policy/SSRF reject must stay 422)",
      "policy reject (E_TARGET_NOT_ALLOWED) downgraded to 200",
      "drop required 'valid' from ValidationData",
      "drop required 'errors' from ValidationData (want [] when valid)",
      "drop required 'valid' from ConnectivityResult",
      "drop required 'errors' from ConnectivityResult (want [] when valid)",
      "retired 50202 re-introduced into x-error-codes",
      "drop E_SOURCE_UNREACHABLE from x-precheck-codes",
      "drop E_TARGET_AUTH_FAILED from x-precheck-codes",
      "x-error-codes code prefix != http",
      "/task-precheck description reintroduces upstream-502 wording",
      "platformState re-introduces 'pending' (D14 forbids)",
      "platformState drops 'failed'",
      "derivation drops 'failed' row",
      "derivation 'failed' loses lastOp guard",
      "intentPriority removed (human-intent priority lost)",
      "lastOpPersistence removed (lastOp not persisted)",
      "state enum (both) re-introduces 'pending'",
      "state enum (both) drops 'failed'",
      "dmEnum drops 'Finished' (control: check_dm_compat guards)",
      "benign: reword a response description",
      "consistent-delete E_FIELD_REQUIRED (registry+enum)",
      "consistent-delete E_PARAM_RANGE (registry+enum)",
      "consistent-delete E_PARAM_ENUM (registry+enum)",
      "consistent-delete E_PARAM_REGEX (registry+enum)",
      "consistent-delete E_PARAM_DEPENDENCY (registry+enum)",
      "consistent-delete E_FIELD_DUPLICATE (registry+enum)",
      "consistent-delete E_FIELD_EXISTENCE (registry+enum)",
      "StateData.allowedActions back to free string[]",
      "drop allowedActions from TaskStatusData (status has no action source)",
      "AllowedAction enum loses 'resume'",
      "AllowedAction enum gains bogus 'restart'",
      "Task response exposes password (D16 regression)",
      "DataSource response exposes target_config.password (D16)",
      "x-credential-handling redline removed",
      "DataSourceWrite.password loses writeOnly (echoable input)"
    ]
  }
}
